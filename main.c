#include <stdio.h>
#include <math.h>

int main(void) {
  int x=3;
  int b;

  printf("o número é %d\n o dobro de %d é %d \n %d elevado a dois é: %d\n a metade de %d é %d",x,x,x*2,x,b=pow(x,2),x,x/2);
  return 0;
}